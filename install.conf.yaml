- defaults:
    link:
        relink: true
        
- clean: ['$HOME']

- link:
    $HOME/.dotfiles: ''
    $HOME/.vim:
    $HOME/.vimrc: vim/vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
